// TODO: Task 17 — Admin OAuth callback
export async function handleAdminCallback(_request: Request, _env: Env): Promise<Response> {
  return new Response('Admin callback not yet implemented', { status: 501 });
}
